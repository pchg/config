#!/bin/bash
if [[ $(whoami) != "root" ]]; then
  echo "Errore: faut faire ça en route."
  exit 1
fi
echo -e "Ouvrons donc une tombe..."
tomb open -f -k /home/pierre/pierre.tomb.key /home/pierre/pierre.tomb /home/pierre/cime_tiers
